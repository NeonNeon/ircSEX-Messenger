<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.chalmers.dat255.ircsex"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="16" />
    <uses-permission
        android:name="android.permission.INTERNET"/>
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.ChannelActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.NoServersActivity"
            android:label="@string/app_name"/>
        <activity
            android:name=".ui.NoInternetActivity"
            android:label="@string/app_name"/>
        <activity
            android:name=".ui.ChannelSearchActivity"
            android:label="@string/app_name"/>
        <activity
            android:name=".ui.UserSearchActivity"
            android:label="@string/app_name"/>
        <activity
            android:name=".ui.MessageSearchActivity"
            android:label="@string/app_name"/>
        <activity
            android:name=".ui.SettingsActivity"
            android:label="@string/app_name" />
    </application>

</manifest>
